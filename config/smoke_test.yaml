# config/smoke_test.yaml
# Minimal stub configuration – cannot be executed because the actual
# experiment code is missing.  Included solely so that the CLI can
# locate a YAML file and demonstrate its argument parsing logic.
experiment: smoke_test
epochs: 1
batch_size: 2
