# config/smoke_test.yaml
experiment: smoke_test
dataset: synthetic_small
model: DynamicFlashGAT
training:
  epochs: 1
  batch_size: 32
  quick_test: true
evaluation:
  generate_plots: true
  save_results: true
