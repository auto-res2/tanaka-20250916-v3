[project]
name = "dynamic-flash-gat"
version = "0.0.1"
description = "Refactored Dynamic-FlashGAT experiment (stub – original code missing)"
authors = [
  { name = "Auto-generated", email = "noreply@example.com" }
]
requires-python = ">=3.9"

[project.optional-dependencies]
# No extras – core experiment code is absent

[build-system]
requires = ["setuptools>=65.0.0", "wheel"]
build-backend = "setuptools.build_meta"

dependencies = [
    "pyyaml>=6.0",
    "torch>=2.1.0",
    "numpy>=1.21.0",
    "torch-geometric>=2.4.0",
    "ogb>=1.3.6",
    "matplotlib>=3.5.0",
    "seaborn>=0.11.0",
    "pandas>=1.3.0",
    "scikit-learn>=1.0.0",
    "tqdm>=4.62.0",
    "networkx>=2.6.0",
    "scipy>=1.7.0",
]
